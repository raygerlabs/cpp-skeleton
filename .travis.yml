language: python
python: "3.7"
os:
  - linux
  - windows
  - osx
      
install:
  - pip3 install cmake conan ninja --upgrade
  - conan user
  - conan profile detect

before_script:
  - cd imagelib

script:
  - conan install . -b missing
  - cmake --workflow --fresh --preset ci
